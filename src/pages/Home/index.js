import React from "react";

import { ScrollView, Text } from "react-native";

export const Home = () => (
  <ScrollView>
    <Text>Olá eu estou Atto AI, seu assistente pessoal!</Text>
  </ScrollView>
);
